version=pmwiki-2.2.43 ordered=1 urlencoded=1
text=(:include {$SiteGroup}.BootstrapSkin-DefaultPageTitle :)(:nl:)(:comment Only the default is included here because it uses bootstrap styles. If you wish to include other page title pages, make sure to refer the default to find out what bootstrap styles to use. :)
