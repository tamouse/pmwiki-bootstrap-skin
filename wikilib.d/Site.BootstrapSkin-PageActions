version=pmwiki-2.2.43 ordered=1 urlencoded=1
text=(:include {$SiteGroup}.BootstrapSkin-DefaultPageActions:)(:nl:)(:comment the default contains the correct class formatting to make the bootstrp navbar work correctly. Look at the include default file to understand the formatting needs before you change this to another form.:)
